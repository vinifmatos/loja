<!-- Page Content -->
<div class="container">
  <%= link_to t('acoes.novo', model: Produto.name), new_admin_produto_path, class: 'btn btn-primary' %>
  <div class="row mt-3">
    <%= render 'admin/partials/grid_produtos', produtos: @produtos %>
  </div>
</div>
