<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="tituloModalLogout"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="tituloModalLogout"><%= t('modal.logout.titulo') %></h5>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body"><%= t('modal.logout.texto') %></div>
      <div class="modal-footer">
        <%= button_tag t('acoes.nao'), type: 'button', data: { dismiss: 'modal' }, class: 'btn btn-secondary' %>
        <%= link_to t('acoes.sim'), destroy_usuario_session_path, method: :delete, class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
</div>